h2mem1 = {
  connectionPool = "HikariCP" //use HikariCP for our connection pool
//  jdbcUrl = "jdbc:h2:~/testdb;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=true;AUTO_SERVER=true"
  jdbcUrl = "jdbc:h2:~/testdb;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;MODE=PostgreSQL"
  properties = {
    driver = "org.postgresql.Driver"
    serverName = "localhost"
    portNumber = "5432"
    databaseName = "testdb"
    user = ""
    password = ""
  }
  numThreads = 10
}
